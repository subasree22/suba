#include <stdio.h>
int main() {
    int a,b;
    int sum;
    printf("\n enter the value : ");
    scanf("%d",&a);
      printf("\n enter the value : ");
    scanf("%d",&b);
    sum=a+b;
    printf("%d+%d=%d",a,b,sum);
}
